# TODO Tasks – Phase 1

Phase 1 Goals

- Set up clean package structure.
- Configure TypeScript, testing, and build tools.
- Define all types, interfaces, and enums.
- Implement configuration and HKDF utilities.
- Prepare testing infrastructure.

Definition of Done (DoD)

- `npm build` (or equivalent) succeeds for the crypto package.
- All TypeScript compilation errors are resolved.
- Tests (even if minimal) run without errors.

Instructions for the AI Agent:

- Do not implement actual encryption/decryption logic yet (that is Phase 2).
- Ask for clarification if the existing base project structure differs significantly from the expected one.

## Task 1: Project Structure & Package Configuration [DONE]

- [x] Create/update/improve the directory structure based on the proposal in the Project Brief:

  ```text
  crypto/
  ├── package.json
  ├── tsconfig.json
  ├── README.md
  ├── src/
  │   ├── index.ts
  │   ├── config.ts
  │   ├── crypto.service.ts
  │   ├── hkdf.ts
  │   ├── testing/
  │   │   ├── index.ts
  │   │   └── test-vectors.ts
  │   └── utils.ts
  └── tests/ (or __tests__)
  ```

- [x] Review and update `package.json` (if needed) to:
  - include `crypto` in workspaces.
  - upgrade dependencies versions to last compatible version.
- [x] Create `crypto/package.json` with:
  - Name: `@cobranza-apps/crypto`
  - Version: `0.1.0`
  - Proper `main`, `types`, `files`, and `exports` fields (support `"./testing"` subpath).
  - Dependencies: `@cobranza-apps/entities` (as peer + regular dependency).
  - Dev dependencies: `typescript`, `@types/node`, `jest`, `ts-jest`, `@types/jest`.
  - Scripts: `build`, `test`, `lint`, `test:watch`.
- [x] Update `tsconfig.json` (strict mode, declaration files, target ES2022, etc.) and fix error:

  ```text
  Option 'moduleResolution=node10' is deprecated and will stop functioning in TypeScript 7.0. Specify compilerOption '"ignoreDeprecations": "6.0"' to silence this error.
  Visit https://aka.ms/ts6 for migration information.
  ```

## Task 2: Core Types & Configuration

- [ ] In `src/config.ts`:
  - Define `EncryptionKey` enum (exactly as specified in the brief).
  - Define `CryptoConfig` interface.
  - Add JSDoc documentation with examples.
- [ ] In `src/hkdf.ts`:
  - Implement `deriveKey(masterKey: string, keyName: string, version?: number): Buffer` using Node.js `crypto.hkdfSync`.
  - Add proper error handling for invalid master key length.
- [ ] In `src/utils.ts`:
  - Add helper functions (e.g., `base64ToBuffer`, `constantTimeCompare`, etc.).

## Task 3: SecureCrypto Core Class (Skeleton)

- [ ] In `src/crypto.service.ts`:
  - Implement `SecureCrypto` class constructor that accepts `CryptoConfig`.
  - Add private properties for derived keys cache (Map).
  - Stub all public methods with proper signatures and JSDoc.
- [ ] In `src/index.ts`:
  - Export all public API (`SecureCrypto`, `EncryptionKey`, `CryptoConfig`, testing utilities).
  - Add barrel exports.

## Task 4: Testing Infrastructure

- [ ] Create `src/testing/index.ts`:
  - Export `SecureCryptoTestModule` (NestJS module if applicable) and `getTestCrypto()` factory.
  - Use fixed, known test keys and salts for deterministic tests.
- [ ] Create `src/testing/test-vectors.ts` with at least 5 test cases (plain text → encrypted + hash pairs).
- [ ] Create initial test file (e.g. `tests/crypto.service.test.ts`) with basic structure (constructor, config validation).

## Task 5: Documentation & Quality

- [ ] Review and update `README.md` that should cover:
  - Purpose and high-level architecture.
  - Installation / usage examples.
  - Configuration (how to pass `masterKey` and `hashSalt` from NestJS `ConfigService`).
  - Security notes.
  - Key rotation overview.
- [ ] Add/Update `.gitignore`, `.eslintrc` (if not present), and basic linting setup.
- [ ] Ensure all files have proper copyright/license headers if required by the project.
