# TODO Tasks – Phase 2

Phase 2 Goals: implement the full cryptographic logic using Master Key + HKDF strategy, complete the `SecureCrypto` class, and ensure everything is secure and well-tested.

Phase 2 Definition of Done (DoD)

- All cryptographic methods are fully implemented and secure.
- All tests pass and cover edge cases.
- README is complete and useful.
- No direct `process.env` reading inside the library.
- Code follows TypeScript strict mode and security best practices.

Instructions for the AI Agent: prioritize security and clarity.

## Task 1: Complete SecureCrypto Implementation

- [ ] In `src/crypto.service.ts`:
  - Finish the constructor: validate `masterKey` (must be 32 bytes after decoding), `hashSalt`, and initialize internal key derivation cache.
  - Implement `deriveKey()` private method using the HKDF utility (cache results by `keyName + version`).
  - Implement `encrypt()`:
    - Use AES-256-GCM.
    - Generate random 12-byte IV.
    - Return proper `EncryptedValue` (with `encryptedData`, `keyName`, `algorithm`, `version`).
  - Implement `decrypt()`:
    - Parse the Base64 `encryptedData` (extract IV, ciphertext, authTag).
    - Use correct key version and throw clear error if key is missing.
  - Implement `hash()` and `verifyHash()` using HMAC-SHA256 + `hashSalt`.
  - Implement `encryptAndHash()` (combine both operations efficiently).
  - Implement helper methods (`hasKey`, `getAvailableKeys`).

Important: Never store the raw master key in memory longer than necessary. Use Buffers and clean up when possible.

## Task 2: Testing (Comprehensive)

- [ ] Expand `tests/crypto.service.test.ts` (or equivalent) with:
  - Constructor validation tests (invalid master key, missing config, etc.).
  - Encryption → Decryption roundtrip tests.
  - Deterministic hashing + verification tests.
  - Different `keyName` / `EncryptionKey` tests.
  - Version handling tests (old vs new version decryption).
  - Error cases (wrong auth tag, corrupted data, missing key).
  - `encryptAndHash` tests.
  - Use test vectors from `src/testing/test-vectors.ts`.
- [ ] Ensure all tests pass with `npm test` or `pnpm test`.
- [ ] Achieve minimum 85% test coverage.

## Task 3: Testing Module Polish

- [ ] Finalize `src/testing/index.ts` and `test-vectors.ts`:
  - Provide stable, fixed test keys/salts.
  - Make it easy to import and use in other services' tests (especially NestJS).

## Task 4: Documentation & Examples

- [ ] Update `README.md` with:
  - Full usage examples (encrypt, decrypt, hash, encryptAndHash).
  - How to configure in NestJS (example using `ConfigService`).
  - Security recommendations (key storage, rotation process, logging rules).
  - Performance notes (caching decrypted values).
- [ ] Add a `CONTRIBUTING.md` section or clear comments about not using environment variables directly inside the library.

## Task 5: Final Quality & Build

- [ ] Ensure the package builds cleanly (`tsc`).
- [ ] Add `exports` field in `package.json` to support subpath imports:

  ```json
  "exports": {
    ".": "./dist/index.js",
    "./testing": "./dist/testing/index.js"
  }
  ```

- [ ] Run linting and fix any issues.
- [ ] Update `package.json` version to `1.0.0` (or `0.1.0` if still experimental).
