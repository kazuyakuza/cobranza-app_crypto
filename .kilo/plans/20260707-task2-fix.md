# Fix Plan — Task 2: Update README File (Code Review)

**Source plan:** `.kilo/plans/20260707-task2-readme.md`
**Target file:** `README.md`
**Review date:** 2026-07-07

## Summary

The new `README.md` faithfully implements the structure and content required by the implementation plan. Only minor deviations/issues were found:

1. Missing encryption output format detail required by plan constraints.
2. Missing API contract reference link to `brief.md` §4 required by Section 3.8.
3. Broken badge anchor (`#status`) points to a heading that does not exist (`## Status / Stability` resolves to `#status--stability`). Markdownlint MD051 flagged this as an invalid link fragment.
4. Minor markdownlint style warnings (MD032 list spacing, MD060 table compact style) — stylistic only; optional.

## Fix Instructions

### Fix 1 — Add encryption output format

**Location:** `## Overview / Purpose` → first bullet under **What it does:**.

**Current bullet:**

```markdown
- **AES-256-GCM** authenticated encryption with per-category **HKDF-SHA256** key derivation.
```

**Replace with:**

```markdown
- **AES-256-GCM** authenticated encryption with per-category **HKDF-SHA256** key derivation. Output format is `IV(12 bytes) + ciphertext + authTag(16 bytes)`, Base64-encoded.
```

**Rationale:** Plan §1.3 constraints and §4 verification checklist explicitly require documenting the output format (`IV(12) + ciphertext + authTag(16)` → Base64).

### Fix 2 — Add API contract reference

**Location:** immediately after the `## API Summary` table.

**Add:**

```markdown
For the full interface contract, see [`brief.md`](./.agent/project-info/brief.md) §4.
```

**Rationale:** Plan §3.8 requires referencing the full interface contract via a link to `brief.md`.

### Fix 3 — Fix broken Status badge anchor

**Location:** header badges (line 7).

**Current:**

```markdown
[![Status](https://img.shields.io/badge/status-WIP%20%28API%20stabilizing%29-yellow)](#status)
```

**Replace with:**

```markdown
[![Status](https://img.shields.io/badge/status-WIP%20%28API%20stabilizing%29-yellow)](#status--stability)
```

**Rationale:** Markdownlint MD051 flagged an invalid link fragment. The existing heading is `## Status / Stability`, which resolves to the anchor `#status--stability`, not `#status`.

### Fix 4 — Optional style polish (not required)

Address markdownlint warnings only if a style pass is desired:

- Add blank lines before/after the bullet lists at lines 16 and 22 (MD032).
- Add spaces around pipes in the API Summary table (MD060).

These are stylistic; plan Step 3 says to fix only true errors, not stylistic hints.

## Verification

- Re-run `vscode-mcp-server_get_diagnostics_code` on `README.md` (severities `[0,1]`); confirm the MD051 invalid-fragment warning is resolved.
- Confirm the encryption output format sentence appears in the Overview section.
- Confirm the API Summary section contains a `brief.md` link.
- Confirm no base-template content remains and all required sections are present.
