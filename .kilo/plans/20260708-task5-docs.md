# Plan — Task 5: Documentation & Quality (4.1 Analysis & Planning)

- **TODO file**: `.agent/todos/20260707/20260707-todo-1.md` → Task 5 (lines 90-99)
- **Plan path**: `.kilo/plans/20260708-task5-docs.md`
- **Date**: 2026-07-08
- **Phase**: Phase 1 (skeleton)
- **Status**: Drafted by architect; pending Plan Agent / user approval

---

## 1. Context & Scope

Task 5 closes Phase 1's "Documentation & Quality" work. The TODO requires:

1. Review and update `README.md` (purpose/architecture, install/usage, ConfigService
   configuration, security notes, key rotation overview).
2. Add/Update `.gitignore`, `.eslintrc` (if not present), basic linting setup.
3. Ensure all files have proper copyright/license headers **if required** by the project.

This is a **documentation + config** task. No `src/` source changes. No new crypto logic.

---

## 2. Pre-Analysis — Current State Assessment

### 2.1 README.md (274 lines) — already comprehensive but Phase-1-inaccurate
Covers all five TODO content areas (Overview/Purpose, Installation, Configuration +
NestJS `ConfigService` guide, Security Best Practices, Key Rotation Procedure) plus
Status, API Summary, Testing, Development, License. **Gap:** usage examples and the API
table present `encrypt`/`decrypt`/`hash`/`verifyHash`/`encryptAndHash` as if they work,
but the actual Phase 1 implementation throws `Error('Not implemented in Phase 1')`
(verified in `src/crypto.service.ts` lines 98-153). Only construction/validation,
`hasKey`, and `getAvailableKeys` are functional.

### 2.2 `.eslintrc.json` — present and functional
Configured in Task 1: `@typescript-eslint/parser` + `@typescript-eslint/recommended` +
`plugin:jest/recommended`, sensible rules, `ignorePatterns` covers `dist/`, `node_modules/`,
`coverage/`, `*.js`. References `./tsconfig.eslint.json` **which exists** (verified).
**No changes required.** Lint setup is complete; implementer only needs to run
`npm run lint` to confirm it passes.

### 2.3 `.gitignore` (40 lines) — appropriate, two minor gaps
Includes `dist/`, `node_modules/`, `.env`, `.env.local`, `.env.production`, logs, IDE,
OS, build artifacts, `.kilo/agent-manager.json`, `.git-credentials`.
- **Gap A:** `coverage/` is ignored by eslint but **not** by `.gitignore` (a `npm test
  --coverage` run would produce an untracked `coverage/` dir at risk of being committed).
- **Gap B:** `.env` variants are listed explicitly (`.env`, `.env.local`,
  `.env.production`) but `.env.development`, `.env.staging`, `.env.test` are not covered.
  A `.env*` glob is safer.

### 2.4 License field inconsistency (quality issue)
- `package.json` -> `"license": "UNLICENSED"` (npm convention for "do not publish /
  proprietary").
- `LICENSE` file -> **The Unlicense** (public domain dedication).
- `README.md` badge + License section -> **Unlicense**.

These three disagree. The actual license is The Unlicense (repo root `LICENSE` file is
canonical). `package.json` should reflect it. `"private": true` stays (package is not
published; the field is informational).

### 2.5 Copyright/license headers on source files — NOT required
Inspected all 9 `src/**/*.ts` files + `tests/crypto.service.spec.ts`: **none** carry
copyright/license headers; they use JSDoc module docs only. The project is dedicated to
the **public domain** via The Unlicense, which **explicitly disclaims copyright
interest**. Adding `Copyright (c) ...` headers would **contradict** the public-domain
dedication. The repo-root `LICENSE` file is the canonical notice. **Decision: do NOT add
headers.** This satisfies TODO item 3 ("...if required by the project") — the conclusion
is they are not required.

### 2.6 docs/ folder gap — OUT OF SCOPE for Task 5 (flagged, deferred)
Brief section 9 calls for "Full documentation in docs folder" + "Step by step library
introduction for AI Agents". Current `docs/` holds only generic base-project guides
(`how-to-set-up-git.md`, `how-to-write-todo-files.md`, `docs/README.md`) — none are
crypto-library-specific. TODO Task 5 bullets are limited to README, `.gitignore`/
`.eslintrc`, and license headers. Creating new `docs/` files is **out of scope** here
(scope discipline + markdown-generation-rule). **Deferred** to a future TODO / the
broader 4.4 Documentation cycle. The README already links the existing `docs/` index.

---

## 3. Key Decisions

| # | Decision | Rationale |
|---|----------|-----------|
| D1 | Update README to explicitly mark Phase 1 functional vs. stubbed methods | Accuracy; examples currently misrepresent runtime behavior |
| D2 | Keep the Phase 2 usage examples (do not delete) and annotate them as the target API | Documents the contract now; avoids a Phase 2 README rewrite |
| D3 | Add `coverage/` and `.env*` glob to `.gitignore` | Prevent accidental commits of coverage output / extra env variants |
| D4 | Change `package.json` `"license": "UNLICENSED"` -> `"license": "Unlicense"` | Align with canonical `LICENSE` file and README; valid SPDX identifier |
| D5 | Do NOT add copyright/license headers to source files | Public-domain dedication (The Unlicense) disclaims copyright; headers would contradict it |
| D6 | Do NOT modify `.eslintrc.json` | Already complete and functional (verified `tsconfig.eslint.json` exists) |
| D7 | Do NOT create new `docs/` files | Out of scope for Task 5; deferred to future TODO / 4.4 cycle |

---

## 4. High-Level Approach

1. Edit `README.md` — add an explicit Phase 1 status block (functional vs. stubbed
   methods), annotate the Usage Examples as the Phase 2 target API, add a "Currently
   functional (Phase 1)" subsection, add a Phase 1 column to the API Summary table, and
   annotate the Testing section re: test-vector placeholders.
2. Edit `.gitignore` — add `coverage/` and replace the explicit `.env*` list with a
   `.env*` glob (keeping a clear comment).
3. Edit `package.json` — fix the `license` field to `"Unlicense"`.
4. Verify: `npm run lint`, `npm run build`, `npm test` all pass (no source/test changes,
   so these are regression checks).
5. Commit the documentation + config changes with one conventional-commit message.

No source files (`src/**`) and no test files (`tests/**`) are modified.

---

## 5. Detailed Implementation Steps

> Execute strictly in order. Verify each edit before moving on. Use
> `vscode-mcp-server_replace_lines_code` / `edit` for exact-string replacements. Read the
> target file immediately before each edit to confirm current content/line numbers.

### Step 0 — Preconditions (implementer, no code changes)
- Confirm current branch is the Phase 1 feature branch (set in Critical Workflow step 2).
- Confirm working tree has no unrelated staged changes (per gitignore-compliance rule).
- Read `README.md`, `.gitignore`, `package.json` fresh to lock exact strings/line numbers.

### Step 1 — `README.md` edits

#### 1a. Replace the "Status / Stability" section (current lines 26-28)
**oldString** (exact):
```
## Status / Stability

Phase 1 — the library API is stabilizing. Algorithms (AES-256-GCM, HKDF-SHA256, HMAC-SHA256) are the current design choice and may evolve before the 1.0 release. The package is consumed as a workspace package (`@cobranza-apps/crypto`) in a single root-level package layout.
```
**newString**:
```
## Status / Stability

> **Phase 1 skeleton.** The public API surface is defined and type-checked, but the
> cryptographic methods are **stubs**. Only construction/config validation, `hasKey`, and
> `getAvailableKeys` are functional today.

| Method | Phase 1 status |
| --- | --- |
| `new SecureCrypto(config)` | Functional — validates `masterKey` (32-byte decode) + `hashSalt` (non-empty) |
| `hasKey(name)` | Functional |
| `getAvailableKeys()` | Functional |
| `encrypt` / `decrypt` / `hash` / `verifyHash` / `encryptAndHash` | Throws `Error('Not implemented in Phase 1')` |

Algorithms (AES-256-GCM, HKDF-SHA256, HMAC-SHA256) are the current design choice and may
evolve before the 1.0 release. The package is consumed as a workspace package
(`@cobranza-apps/crypto`) in a single root-level package layout.
```

#### 1b. Add a Phase 1 callout immediately after the `## Usage Examples` header (current line 72)
Insert between the header and the first `### Encrypt / Decrypt` subsection.
**oldString** (exact):
```
## Usage Examples

### Encrypt / Decrypt
```
**newString**:
```
## Usage Examples

> **Phase 1 note:** The examples below document the **target API** for Phase 2. In the
> current Phase 1 skeleton, `encrypt`, `decrypt`, `hash`, `verifyHash`, and
> `encryptAndHash` throw `Error('Not implemented in Phase 1')`. See
> [Status / Stability](#status--stability). For what works **today**, see
> [Currently functional (Phase 1)](#currently-functional-phase-1).

### Encrypt / Decrypt
```

#### 1c. Add a "Currently functional (Phase 1)" subsection at the end of Usage Examples
Insert it **after** the "Key introspection" subsection (current lines 98-103) and
**before** the `## API Summary` header (current line 105).
**oldString** (exact):
```
### Key introspection

```typescript
crypto.hasKey('pii');             // true
crypto.getAvailableKeys();        // ['pii','company_pii','bank_data','notification','general']
```

## API Summary
```
**newString**:
```
### Key introspection

```typescript
crypto.hasKey('pii');             // true
crypto.getAvailableKeys();        // ['pii','company_pii','bank_data','notification','general']
```

### Currently functional (Phase 1)

Construction, config validation, and key introspection work today (no cryptography is
performed):

```typescript
import { SecureCrypto, EncryptionKey } from '@cobranza-apps/crypto';

const crypto = new SecureCrypto({
  masterKey: process.env.COBRANZA_CRYPTO_MASTER_KEY!, // base64, decodes to 32 bytes
  hashSalt:  process.env.COBRANZA_CRYPTO_HASH_SALT!,  // base64, non-empty
  currentVersion: 1,
  defaultKeyName: EncryptionKey.PII,
});

crypto.hasKey('pii');        // => true
crypto.hasKey('unknown');    // => false
crypto.getAvailableKeys();   // => ['pii','company_pii','bank_data','notification','general']
```

## API Summary
```

#### 1d. Add a "Phase 1" column to the API Summary table (current lines 107-116)
**oldString** (exact, the full table):
```
| Method | Parameters | Returns | Description |
|--------|-----------|---------|-------------|
| `constructor` | `config: CryptoConfig` | `SecureCrypto` | Creates a new instance with the given configuration |
| `encrypt` | `plaintext: string, keyName: EncryptionKey` | `EncryptedValue` | Encrypts a string using AES-256-GCM with HKDF-derived key |
| `decrypt` | `data: EncryptedValue` | `string` | Decrypts an `EncryptedValue`, supporting any version with an available key |
| `hash` | `plaintext: string` | `string` | Produces a deterministic HMAC-SHA256 hash |
| `verifyHash` | `plaintext: string, hash: string` | `boolean` | Constant-time hash verification |
| `encryptAndHash` | `plaintext: string, keyName: EncryptionKey` | `{ encrypted: EncryptedValue, hash: string }` | Combined encryption + hashing for indexed PII fields |
| `hasKey` | `name: string` | `boolean` | Checks whether a key derivation config exists for the given `name` |
| `getAvailableKeys` | — | `string[]` | Returns all configured key names |
```
**newString**:
```
| Method | Parameters | Returns | Description | Phase 1 |
|--------|-----------|---------|-------------|---------|
| `constructor` | `config: CryptoConfig` | `SecureCrypto` | Creates a new instance with the given configuration | functional |
| `encrypt` | `plaintext: string, keyName: EncryptionKey` | `EncryptedValue` | Encrypts a string using AES-256-GCM with HKDF-derived key | stub (Phase 2) |
| `decrypt` | `data: EncryptedValue` | `string` | Decrypts an `EncryptedValue`, supporting any version with an available key | stub (Phase 2) |
| `hash` | `plaintext: string` | `string` | Produces a deterministic HMAC-SHA256 hash | stub (Phase 2) |
| `verifyHash` | `plaintext: string, hash: string` | `boolean` | Constant-time hash verification | stub (Phase 2) |
| `encryptAndHash` | `plaintext: string, keyName: EncryptionKey` | `{ encrypted: EncryptedValue, hash: string }` | Combined encryption + hashing for indexed PII fields | stub (Phase 2) |
| `hasKey` | `name: string` | `boolean` | Checks whether a key derivation config exists for the given `name` | functional |
| `getAvailableKeys` | — | `string[]` | Returns all configured key names | functional |

> `functional` = works in Phase 1 · `stub (Phase 2)` = throws
> `Error('Not implemented in Phase 1')`, implemented in Phase 2.
```

#### 1e. Annotate the Testing section (current lines 209-234)
Add a Phase 1 note after the "Consumer testing" code block and clarify the test-vector
placeholders. Insert a callout immediately after the consumer-testing code fence
(current line 221) and before the bullet list (current line 223).
**oldString** (exact):
```
const crypto = getTestCrypto();
const { encrypted, hash } = crypto.encryptAndHash('test@example.com', EncryptionKey.PII);
```

- `getTestCrypto()` returns a `SecureCrypto` with fixed, deterministic keys — safe to publish; never usable in production.
- `test-vectors.ts` provides deterministic input/output pairs for reliable assertions across versions.
- `SecureCryptoTestModule` is a NestJS dynamic module for use in `Test.createTestingModule`.
```
**newString**:
```
const crypto = getTestCrypto();
const { encrypted, hash } = crypto.encryptAndHash('test@example.com', EncryptionKey.PII);
```

> **Phase 1 note:** `getTestCrypto()` constructs a valid instance and key-introspection
> (`hasKey` / `getAvailableKeys`) works today. The `encryptAndHash` call above is the
> **Phase 2 target** — it currently throws `Error('Not implemented in Phase 1')`. In
> `test-vectors.ts`, `expectedEncrypted` and `expectedHash` are `PLACEHOLDER_PHASE2`
> sentinels until the crypto methods are implemented.

- `getTestCrypto()` returns a `SecureCrypto` with fixed, deterministic keys — safe to publish; never usable in production.
- `test-vectors.ts` provides deterministic input/output pairs for reliable assertions across versions.
- `SecureCryptoTestModule` is a NestJS-friendly provider config (spreadable into `Test.createTestingModule`); it does not require `@nestjs/testing` as a dependency of this library.
```

#### 1f. README edits summary
- Sections touched: Status / Stability; Usage Examples (callout + new subsection); API
  Summary (table column + legend); Testing (Phase 1 note + wording fix).
- TOC unchanged (subsections are not listed in the TOC; no top-level sections added or
  removed).
- No content removed; all original Phase 2 examples preserved as the documented target
  API.

### Step 2 — `.gitignore` edits

#### 2a. Replace the explicit `.env` list with a `.env*` glob
**oldString** (exact, current lines 20-24):
```
# Environment variables
.env
.env.local
.env.production
```
**newString**:
```
# Environment variables (all variants: .env, .env.local, .env.production, .env.test, ...)
.env*
```

#### 2b. Add `coverage/` to the build-artifacts block
**oldString** (exact, current lines 29-31):
```
# Build artifacts (generic)
build/
dist/
```
**newString**:
```
# Build artifacts (generic)
build/
dist/
coverage/
```

### Step 3 — `package.json` license field fix

#### 3a. Align the license field with the canonical `LICENSE` file
**oldString** (exact):
```
  "license": "UNLICENSED",
```
**newString**:
```
  "license": "Unlicense",
```
- `"private": true` is left untouched (package is not published).
- `Unlicense` is a valid SPDX identifier matching the repo-root `LICENSE` (The Unlicense,
  public domain). This removes the `UNLICENSED` (proprietary) vs. `Unlicense` (public
  domain) contradiction.

### Step 4 — Verification commands (implementer runs; expected to pass)

Run each independently (no compound commands). These are regression checks since no
source/test files changed.

1. `npm run lint` — ESLint over `.` for `.ts`; expect 0 errors.
   (If pre-existing warnings appear, they are out of scope — do not fix unrelated
   warnings in this task.)
2. `npm run build` — `tsc` -> `dist/`; expect success (no `src/` changes).
3. `npm test` — `jest --passWithNoTests`; expect the existing Phase 1 skeleton suite
   (`tests/crypto.service.spec.ts`) to pass.
4. `npm run clean` is **not** required unless `dist/` needs clearing before build.

If any command fails, stop and report — do not attempt to fix source code in this task
(that would be scope creep; flag to caller).

### Step 5 — Commit (implementer)

- Stage **only**: `README.md`, `.gitignore`, `package.json`.
- Before committing, run `git status` and confirm no `.gitignore`-matching files (e.g.
  `dist/`, `node_modules/`, `coverage/`, `.env*`) are staged (gitignore-compliance rule).
- Commit message (single conventional commit):

```
docs: align README with Phase 1 skeleton status; fix license field and .gitignore

- README: mark encrypt/decrypt/hash/verifyHash/encryptAndHash as Phase 2 stubs
  (throw 'Not implemented in Phase 1'); add functional-vs-stub status table,
  a Phase 1 callout on Usage Examples, a 'Currently functional (Phase 1)'
  subsection, a Phase 1 column on the API Summary table, and a Testing note
  on test-vector placeholders.
- .gitignore: add coverage/; replace explicit .env list with .env* glob.
- package.json: correct license field from 'UNLICENSED' to 'Unlicense' to
  match the canonical LICENSE file (The Unlicense / public domain).
- No source files modified; no copyright headers added (public-domain
  dedication disclaims copyright interest; LICENSE file is canonical).
```

---

## 6. Out of Scope / Deferred

- **Crypto-library-specific `docs/` files** (brief section 9 "Full documentation in docs
  folder" + "step by step library introduction for AI Agents"): current `docs/` holds
  only generic base-project guides. Deferred to a future TODO / the 4.4 Documentation
  cycle.
- **Source-file copyright/license headers**: not added (D5).
- **`.eslintrc.json` changes**: none (D6); already complete.
- **Any `src/**` or `tests/**` modification**: none.
- **Key rotation scheme details, algorithm finalization**: Phase 2 (per
  `.agent/project-info/context.md` open questions).

---

## 7. Verification Checklist (for 4.5 Verification step)

- [ ] `README.md` "Status / Stability" shows the functional-vs-stub table.
- [ ] `README.md` Usage Examples has the Phase 1 callout and a "Currently functional
      (Phase 1)" subsection.
- [ ] `README.md` API Summary table has a "Phase 1" column + legend.
- [ ] `README.md` Testing section has the Phase 1 note + placeholder explanation.
- [ ] `README.md` still covers: purpose/architecture, install/usage, ConfigService
      configuration, security notes, key rotation (TODO Task 5 content requirements).
- [ ] `.gitignore` contains `coverage/` and a `.env*` glob.
- [ ] `package.json` `"license"` equals `"Unlicense"`; `"private": true` unchanged.
- [ ] No `src/**` or `tests/**` files modified.
- [ ] No copyright/license headers added to any source file.
- [ ] `npm run lint`, `npm run build`, `npm test` all pass.
- [ ] Single commit with the message in Step 5; only 3 files staged.

---

## 8. Risks & Mitigations

| Risk | Mitigation |
|------|-----------|
| Exact-string edits fail due to whitespace/line-ending drift | Implementer reads each file fresh before editing; uses unique surrounding context |
| Markdown table rendering breaks with the new column | Keep column count consistent (5 cols); the legend uses a blockquote, not a table |
| `.env*` glob accidentally ignores a tracked `.env.example` | None tracked today (verified: no `.env*` files in repo). If one is added later, use `!.env.example` negation — out of scope now |
| `npm run lint` surfaces pre-existing warnings | Out of scope; report only, do not fix unrelated warnings |

---

## 9. Summary for Caller

- **Plan path**: `.kilo/plans/20260708-task5-docs.md`
- **Files to change (3)**: `README.md`, `.gitignore`, `package.json`.
- **Files NOT changed**: all `src/**`, `tests/**`, `.eslintrc.json`, `LICENSE`, docs/.
- **Key decisions**: D1-D7 above (notably: no copyright headers — public domain; fix
  `package.json` license to `Unlicense`; README made Phase-1-accurate; `.eslintrc` left
  as-is; new `docs/` content deferred).
- **Approval gate**: this plan is returned for Plan Agent / user approval before 4.2
  Implementation.
