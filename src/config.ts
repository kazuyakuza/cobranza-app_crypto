/**
 * Configuration interfaces and EncryptionKey enum.
 *
 * Defines:
 * - {@link CryptoConfig} — constructor options for SecureCrypto (masterKey, hashSalt, version).
 * - {@link EncryptionKey} — key category enum (PII, COMPANY_PII, BANK_DATA, NOTIFICATION, GENERAL).
 */

export {};
