/**
 * HKDF key derivation.
 *
 * Internal module that derives per-category encryption keys from a single
 * master key using HKDF-SHA256.
 *
 * Parameters:
 * - salt: optional fixed salt or empty
 * - info: "cobranza-encryption-v1:${keyName}"
 * - keyLength: 32 bytes
 */

export {};
