/**
 * Testing module exports.
 *
 * Re-exports test helpers for consumers:
 * - {@link SecureCryptoTestModule} — NestJS-friendly test module
 * - {@link getTestCrypto} — factory returning a pre-configured SecureCrypto instance with fixed test keys
 */

export {};
