/**
 * Deterministic test vectors.
 *
 * Fixed input/output pairs for SecureCrypto operations (encrypt, decrypt, hash,
 * verifyHash). Used by unit tests to guarantee reproducible results across runs.
 */

export {};
